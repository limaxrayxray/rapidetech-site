# Copie vers /etc/rapidetech-deploy.env (chmod 600) et remplis DEPLOY_SECRET.
# Le MÊME secret doit être mis dans l'en-tête Authorization du Flow Directus.
DEPLOY_SECRET=remplace-moi-par-un-secret-long-et-aleatoire
DEPLOY_BIND=172.18.0.1
DEPLOY_PORT=8787
DEPLOY_DEBOUNCE_MS=8000
