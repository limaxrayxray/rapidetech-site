// Copie de référence (handoff) — `export` retiré pour éviter la collision avec le design system source.
import React from 'react';

const cssId = 'rt-css-logo';
const css = `
.rt-logo {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 18px;
  letter-spacing: 0.04em;
  color: var(--text-primary);
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  text-transform: uppercase;
}
.rt-logo i {
  width: 10px;
  height: 10px;
  background: var(--accent);
  display: inline-block;
}
`;

// Identité officielle : carré vert #6BC31A + RAPIDETECH en Clash Display 600.
// Ce composant texte est réservé aux interfaces où Clash Display est chargée.
// Pour un export autonome, utiliser les SVG horizontaux du dossier brand/svg :
// leur mot-symbole est converti en tracés et ne dépend d'aucune police installée.
// Pour le courriel, ne pas charger Clash Display : utiliser la pile de repli
// Aptos Display, Century Gothic, Trebuchet MS, Arial, sans-serif.

function inject() {
  if (typeof document !== 'undefined' && !document.getElementById(cssId)) {
    const el = document.createElement('style');
    el.id = cssId;
    el.textContent = css;
    document.head.appendChild(el);
  }
}

function Logo({ href = '#', style }) {
  inject();
  return <a className="rt-logo" href={href} style={style}><i></i>RAPIDETECH</a>;
}
